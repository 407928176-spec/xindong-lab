# ⚠️ 一般玩家不需要这个文件。
#
# 正常玩法是：双击根目录的「心动实验室.exe」/「心动实验室.command」，浏览器会自动打开配置向导，
# 在网页上填大模型信息即可，配置会存到 backend/data/llm_config.json。
#
# 这个文件是给「不想用网页向导」的进阶用户准备的：比如自动化部署、多环境切换，
# 或者想把配置纳入自己的管理体系。用法是复制本文件为同目录下的 .env 再填值。
# 环境变量的优先级高于网页向导存的配置。
#
# .env 已被 .gitignore 忽略，但仍请注意：里面是明文 Key，不要提交、不要外发。

# ── 大模型（OpenAI 兼容接口）─────────────────────────────────────────────
# 任何 OpenAI 兼容的服务都可以：OpenAI / DeepSeek / 火山方舟 / 通义 / Ollama / vLLM …
# 常见 Base URL：
#   OpenAI    https://api.openai.com/v1
#   DeepSeek  https://api.deepseek.com/v1
#   火山方舟   https://ark.cn-beijing.volces.com/api/v3
#   Ollama    http://localhost:11434/v1
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx
# LLM_MODEL=gpt-4o

# 可选：负责其他环节的模型（人设创建对话 / 状态评估 / 结局文案 / 长记忆摘要）。
# 留空则与 LLM_MODEL 相同。这些调用都在角色回复呈现之后于后台执行，耗时不影响体感，
# 因此可以选用能力更强的旗舰模型，无需顾虑速度。
# LLM_AUX_MODEL=

# ── 数据库（可选）───────────────────────────────────────────────────────
# 不设置时默认使用 backend/data/app.db
# DATABASE_URL=sqlite:///./data/app.db
