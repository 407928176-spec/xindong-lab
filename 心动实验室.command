#!/usr/bin/env bash
# 心动实验室 macOS 一键启动。
#
# 双击这个文件即可启动游戏（Finder 会用终端打开它）。真正的启动逻辑都在
# start.sh 里，这个文件只是一个好认、双击可用的入口——避免玩家在解压后
# 一堆文件里翻找该点哪一个。
#
# 首次双击如果被系统拦截（"无法打开，因为无法验证开发者"），
# 在 Finder 里右键这个文件 → 打开，确认一次即可，以后就能直接双击了。
set -euo pipefail
cd "$(dirname "$0")"
bash start.sh
